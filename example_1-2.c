#include <stdio.h>

int main()
{
	int i, sum;

	sum = i * 50;
	i = 101;

	printf("iterator: %d\n", i);
	printf("sum: %d\n", sum);

	/*
	* Assume that some operation is performed
	* using the previously computed results 'sum' and 'i' here
	*/

	return 0;
}